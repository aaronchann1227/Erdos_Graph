// implementation

#include "Graph.h"

//unordered_map< Vertex, vector<Edge*> > vertexMap
// vector<Edge*> edges;

ErdosGraph::ErdosGraph(std::vector< std::vector<std::string> > erdosVec) {
    // for (size_t i = 0; i < erdosVec.size(); i++) {
    //     Vertex erdos1(erdosVec[i][0]);
    //     for (size_t j = 1; j < erdosVec[i].size(); j++) {
            
    //     }
    // }
}

